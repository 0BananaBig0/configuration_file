set home $::env(HOME)
set pattern "$home/oasys_rtl_qs_ekit/libs/fastscan/*.fslib"
set matches [glob -nocomplain $pattern]
if {[llength $matches] > 0} {
    read_cell_library "$home/oasys_rtl_qs_ekit/libs/fastscan/*.fslib"
}
